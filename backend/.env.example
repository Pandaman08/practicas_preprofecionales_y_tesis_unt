DATABASE_URL="postgresql://postgres:kali123@localhost:5432/practicas_tesis_db?schema=public"

JWT_SECRET="replace_with_strong_secret"
JWT_EXPIRES_IN="7d"

PORT=3001
NODE_ENV=development
FRONTEND_URL="http://localhost:3000"

# RUN_SEED solo se usa en arranque docker para sembrar datos de prueba.
# En producción establezca RUN_SEED=false para evitar reset de datos.
RUN_SEED=false
